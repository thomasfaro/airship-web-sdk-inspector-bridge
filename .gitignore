.DS_Store
node_modules/

# Installed by the launcher, inside the folder, on the machine that runs it.
.node/
.adb/
.update-*

# Drop this file in to keep a folder on the version it has.
.no-auto-update
